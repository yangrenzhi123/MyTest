server:
  port: 8762
spring:
  application:
    name: mmb-eureka-client-provider
  cloud:
    config:
      uri: http://localhost:8889
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtual-host: /
endpoints:
  shutdown:
    enabled: true