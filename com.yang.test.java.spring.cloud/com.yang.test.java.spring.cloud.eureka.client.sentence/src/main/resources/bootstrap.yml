server:
  port: 8762
spring:
  application:
    name: mmb-eureka-client-provider
  cloud:
    config:
      uri: http://localhost:8889
endpoints:
  shutdown:
    enabled: true
eureka:
  instance:
    prefer-ip-address: true
words: you,me,he8r