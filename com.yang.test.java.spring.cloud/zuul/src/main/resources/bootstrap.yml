spring:
  application:
    name: testZuul
server:
  port: 3106
  tomcat:
    basedir: /tmp
zuul:
  routes:
    api-a:
      path: /**
      serviceId: mmb-eureka-client-provider
  semaphore:
    max-semaphores: 10000
ribbon:
  ReadTimeout: 10000